exchange_id = "bybit"
support_level = "full"
max_streams_per_conn = 10

[safety_profile]
max_streams_per_conn = 8
max_symbols_per_conn = 16

[rate]
messages_per_second = 1

[heartbeat]
ping_interval_secs = 20
idle_timeout_secs = 30
