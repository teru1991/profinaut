[package]
name = "ucel-transport"
version = "0.1.0"
edition = "2021"

[dependencies]
ucel-core = { path = "../ucel-core" }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true, features = ["time", "sync"] }
bytes = { workspace = true }
tracing = { workspace = true }

rand = "0.8"
async-trait = "0.1"
futures-util = "0.3"
base64 = "0.22"

# IMPORTANT: permessage-deflate ON (OKX)
tokio-tungstenite = { version = "0.24", features = ["deflate"] }

ucel-journal = { path = "../ucel-journal" }
ucel-subscription-store = { path = "../ucel-subscription-store" }
ucel-ws-rules = { path = "../ucel-ws-rules" }

[dev-dependencies]
tempfile = "3"
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "time", "net"] }
futures-util = "0.3"
serde_json = { workspace = true }
async-trait = "0.1"

# IMPORTANT: permessage-deflate ON (tests too)
tokio-tungstenite = { version = "0.24", features = ["deflate"] }

ucel-subscription-planner = { path = "../ucel-subscription-planner" }
