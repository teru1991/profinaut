# Issuer Site Discovery Flow

1. official metadata seed lookup
2. inventory seed fallback
3. deterministic root traversal with depth/page caps
4. build `IssuerSiteProfile`
5. discover documents via HTML/feed
6. list/fetch artifacts with source-page provenance

No search engine path is allowed.
