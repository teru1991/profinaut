# US Official Identity Mapping

- `us_cik_like` => primary canonical issuer key seed
- `ticker_like` => lookup alias (CIK resolution)
- `exchange_ticker_like` => secondary market alias
- `url_like` => filing locator alias

All aliases must carry provenance and source scope.
