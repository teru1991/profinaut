
## Task F note (mock mode)

Mock mode can emulate Mongo downtime with `--mock-mongo-down-ms` without requiring a running Mongo server. Use this to validate spool/replay and dedup metric movement locally before integrating with real Mongo.
