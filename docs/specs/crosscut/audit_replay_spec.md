# Audit Replay — Crosscut Spec (CANONICAL)

This is the canonical crosscut spec for audit replay functionality.
All audit events MUST be replayable from the stored event log.

See also:
- `docs/contracts/audit_event.schema.json` — audit event contract
- `docs/contracts/replay_pointers.schema.json` — replay pointer contract
