# ir_normalization_compat_policy_v1
Schema version follows major/minor/patch. Major change is breaking and must fail compatibility gate.
Fixtures are text-only; PDF/ZIP bytes are generated at runtime.
