# WebSocket Subscriptions Catalog (Official) — Deribit

| id | ws_url | channel | subscribe.template | message.shape | message.fields | heartbeat.type | auth.type | restrictions | notes | source_url |
|---|---|---|---|---|---|---|---|---|---|---|
| ws.public.sub.book.instrument.raw | wss://www.deribit.com/ws/api/v2 | book.{instrument_name}.{group}.{depth}.{interval} | {"jsonrpc":"2.0","id":1,"method":"public/subscribe","params":{"channels":["book.BTC-PERPETUAL.raw"]}} | object | params(req):object; params.channel(req):string; params.data(req):object; params.data.type(opt):string; params.data.timestamp(req):int(ms); params.data.bids(opt):array<array>; params.data.asks(opt):array<array> | ping-pong | none | obey channel and message rate recommendations | order-book incremental channel family | https://docs.deribit.com/articles/notifications |
| ws.public.sub.ticker.instrument.interval | wss://www.deribit.com/ws/api/v2 | ticker.{instrument_name}.{interval} | {"jsonrpc":"2.0","id":1,"method":"public/subscribe","params":{"channels":["ticker.BTC-PERPETUAL.100ms"]}} | object | params(req):object; params.channel(req):string; params.data(req):object; params.data.instrument_name(req):string; params.data.last_price(opt):float; params.data.mark_price(opt):float; params.data.best_bid_price(opt):float; params.data.best_ask_price(opt):float; params.data.timestamp(req):int(ms) | ping-pong | none | interval variants per official docs | best for near-real-time ticker streaming | https://docs.deribit.com/articles/notifications |
| ws.public.sub.trades.instrument.interval | wss://www.deribit.com/ws/api/v2 | trades.{instrument_name}.{interval} | {"jsonrpc":"2.0","id":1,"method":"public/subscribe","params":{"channels":["trades.BTC-PERPETUAL.raw"]}} | object | params(req):object; params.channel(req):string; params.data(req):array<object>; trade_id(req):string; price(req):float; amount(req):float; direction(opt):string; timestamp(req):int(ms) | ping-pong | none | raw/aggregated intervals per channel docs | public trade tape | https://docs.deribit.com/articles/notifications |
| ws.public.sub.deribit_price_index.index | wss://www.deribit.com/ws/api/v2 | deribit_price_index.{index_name} | {"jsonrpc":"2.0","id":1,"method":"public/subscribe","params":{"channels":["deribit_price_index.btc_usd"]}} | object | params(req):object; params.channel(req):string; params.data(req):object; params.data.price(req):float; params.data.timestamp(req):int(ms); params.data.index_name(req):string | ping-pong | none | index naming per docs | underlying index updates | https://docs.deribit.com/articles/notifications |
| ws.public.sub.chart.instrument.resolution | wss://www.deribit.com/ws/api/v2 | chart.trades.{instrument_name}.{resolution} | {"jsonrpc":"2.0","id":1,"method":"public/subscribe","params":{"channels":["chart.trades.BTC-PERPETUAL.1"]}} | object | params(req):object; params.channel(req):string; params.data(req):object; params.data.tick(req):int(ms); params.data.open(req):float; params.data.high(req):float; params.data.low(req):float; params.data.close(req):float; params.data.volume(req):float | ping-pong | none | resolution set constrained by docs | streaming OHLC updates | https://docs.deribit.com/articles/notifications |
| ws.private.sub.user.orders.instrument.raw | wss://www.deribit.com/ws/api/v2 | user.orders.{instrument_name}.{interval} | {"jsonrpc":"2.0","id":1,"method":"private/subscribe","params":{"channels":["user.orders.BTC-PERPETUAL.raw"]}} | object | params(req):object; params.channel(req):string; params.data(req):object; params.data.order_id(req):string; params.data.order_state(req):string; params.data.instrument_name(req):string; params.data.price(opt):float; params.data.amount(opt):float | ping-pong | token | requires private auth token | private order lifecycle stream | https://docs.deribit.com/articles/notifications |
| ws.private.sub.user.trades.instrument.interval | wss://www.deribit.com/ws/api/v2 | user.trades.{instrument_name}.{interval} | {"jsonrpc":"2.0","id":1,"method":"private/subscribe","params":{"channels":["user.trades.BTC-PERPETUAL.raw"]}} | object | params(req):object; params.channel(req):string; params.data(req):array<object>; trade_id(req):string; order_id(opt):string; instrument_name(req):string; price(req):float; amount(req):float; direction(opt):string; timestamp(req):int(ms) | ping-pong | token | requires private auth token | private fills/trades stream | https://docs.deribit.com/articles/notifications |
| ws.private.sub.user.portfolio.currency | wss://www.deribit.com/ws/api/v2 | user.portfolio.{currency} | {"jsonrpc":"2.0","id":1,"method":"private/subscribe","params":{"channels":["user.portfolio.btc"]}} | object | params(req):object; params.channel(req):string; params.data(req):object; params.data.currency(req):string; params.data.balance(opt):float; params.data.equity(opt):float; params.data.available_funds(opt):float; params.data.timestamp(req):int(ms) | ping-pong | token | requires private auth token | portfolio/risk updates | https://docs.deribit.com/articles/notifications |
| ws.private.sub.user.changes.instrument.raw | wss://www.deribit.com/ws/api/v2 | user.changes.{instrument_name}.{interval} | {"jsonrpc":"2.0","id":1,"method":"private/subscribe","params":{"channels":["user.changes.BTC-PERPETUAL.raw"]}} | object | params(req):object; params.channel(req):string; params.data(req):object; params.data.orders(opt):array<object>; params.data.trades(opt):array<object>; params.data.positions(opt):array<object>; params.data.timestamp(req):int(ms) | ping-pong | token | requires private auth token | consolidated private delta feed | https://docs.deribit.com/articles/notifications |
