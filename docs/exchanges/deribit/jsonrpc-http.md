# JSON-RPC over HTTP Catalog (Official) — Deribit

| id | base_url | transport | version | method | operation | auth.type | params | result.shape | result.fields | errors.shape | rate_limit | notes | source_url |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| jsonrpc.http.public.market_data.public_get_instruments | https://www.deribit.com/api/v2 | http | v2 | public/get_instruments | list instruments by currency/kind | none | currency(req):string; kind(opt):string; expired(opt):bool | array<object> | instrument_name(req):string; kind(req):string; base_currency(req):string; quote_currency(req):string; settlement_currency(req):string; tick_size(req):float; expiration_timestamp(opt):int(ms) | object | see rate-limit article | HTTP JSON-RPC supports same method surface as WS RPC (without subscription push) | https://docs.deribit.com/api-reference/market-data/public-get_instruments |
| jsonrpc.http.public.market_data.public_ticker | https://www.deribit.com/api/v2 | http | v2 | public/ticker | single instrument ticker snapshot | none | instrument_name(req):string | object | instrument_name(req):string; last_price(opt):float; best_bid_price(opt):float; best_ask_price(opt):float; mark_price(opt):float; timestamp(req):int(ms) | object | see rate-limit article | request/response mode | https://docs.deribit.com/api-reference/market-data/public-ticker |
| jsonrpc.http.public.market_data.public_get_order_book | https://www.deribit.com/api/v2 | http | v2 | public/get_order_book | order book snapshot | none | instrument_name(req):string; depth(opt):int | object | timestamp(req):int(ms); bids(req):array<array>; asks(req):array<array>; instrument_name(req):string; state(opt):string | object | see rate-limit article | pull snapshot for reconciliation | https://docs.deribit.com/api-reference/market-data/public-get_order_book |
| jsonrpc.http.public.market_data.public_get_tradingview_chart_data | https://www.deribit.com/api/v2 | http | v2 | public/get_tradingview_chart_data | historical chart bars | none | instrument_name(req):string; start_timestamp(req):int(ms); end_timestamp(req):int(ms); resolution(req):string | object | ticks(req):array<int(ms)>; open(req):array<float>; high(req):array<float>; low(req):array<float>; close(req):array<float>; volume(req):array<float> | object | see rate-limit article | historical extraction | https://docs.deribit.com/api-reference/market-data/public-get_tradingview_chart_data |
| jsonrpc.http.private.auth.public_auth | https://www.deribit.com/api/v2 | http | v2 | public/auth | acquire bearer token for private HTTP calls | none | grant_type(req):string; client_id(opt):string; client_secret(opt):string; timestamp(opt):int(ms); nonce(opt):string; data(opt):string; signature(opt):string | object | access_token(req):string; expires_in(req):int(s); refresh_token(opt):string; scope(opt):string; token_type(req):string | object | see rate-limit article | auth request itself is public namespace | https://docs.deribit.com/api-reference/authentication/public-auth |
| jsonrpc.http.private.account.private_get_account_summary | https://www.deribit.com/api/v2 | http | v2 | private/get_account_summary | account balances/risk summary | token | currency(req):string; extended(opt):bool | object | equity(req):float; balance(req):float; available_funds(req):float; maintenance_margin(opt):float; initial_margin(opt):float | object | see rate-limit article | pass bearer token per official auth docs | https://docs.deribit.com/api-reference/account-management/private-get_account_summary |
| jsonrpc.http.private.trading.private_buy | https://www.deribit.com/api/v2 | http | v2 | private/buy | place buy order | token | instrument_name(req):string; amount(req):float; type(req):string; price(opt):float; time_in_force(opt):string; post_only(opt):bool; reduce_only(opt):bool; label(opt):string | object | order(req):object; trades(opt):array<object> | object | see rate-limit article | HTTP transport order entry | https://docs.deribit.com/api-reference/private-trading/private-buy |
| jsonrpc.http.private.trading.private_sell | https://www.deribit.com/api/v2 | http | v2 | private/sell | place sell order | token | instrument_name(req):string; amount(req):float; type(req):string; price(opt):float; time_in_force(opt):string; post_only(opt):bool; reduce_only(opt):bool; label(opt):string | object | order(req):object; trades(opt):array<object> | object | see rate-limit article | HTTP transport order entry | https://docs.deribit.com/api-reference/private-trading/private-sell |
| jsonrpc.http.private.trading.private_cancel | https://www.deribit.com/api/v2 | http | v2 | private/cancel | cancel order | token | order_id(req):string | object | order(req):object; trades(opt):array<object> | object | see rate-limit article | HTTP cancel flow | https://docs.deribit.com/api-reference/private-trading/private-cancel |
