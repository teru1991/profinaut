# docs/runbooks（運用手順 / 可変）

このディレクトリは **Runbook（手順）** を管理します。  
Core Spec（固定仕様）を運用に落とすための具体手順を、更新可能な形で保持します。

## ルール
- Runbookは手順であり「契約の正」ではない
- ただし、手順で参照する構造（SafetyState/AuditEvent/SupportBundle等）は SSOT に従う
