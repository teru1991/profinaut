diff --git a/ucel/crates/ucel-cex-sbivc/src/lib.rs b/ucel/crates/ucel-cex-sbivc/src/lib.rs
index 28d51e9..a95a1c7 100644
--- a/ucel/crates/ucel-cex-sbivc/src/lib.rs
+++ b/ucel/crates/ucel-cex-sbivc/src/lib.rs
@@ -1,6 +1,7 @@
 use serde::{Deserialize, Serialize};
 use std::collections::VecDeque;
 use std::sync::atomic::{AtomicU64, Ordering};
 use std::sync::Arc;
 use tokio::sync::Mutex;
@@ -8,6 +9,7 @@ use ucel_core::{
     Decimal, ErrorCode, OrderBookDelta, OrderBookLevel, OrderBookSnapshot, Side, TradeEvent,
     UcelError,
 };
+use ucel_core::decimal::serde::deserialize_decimal_guarded_default;
 
@@ -384,8 +385,10 @@ struct TradeWire {
     trade_id: String,
+    #[serde(deserialize_with = "deserialize_decimal_guarded_default")]
     price: Decimal,
+    #[serde(deserialize_with = "deserialize_decimal_guarded_default")]
     qty: Decimal,
     side: Side,
 }
