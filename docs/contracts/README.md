# docs/contracts/ — Contract SSOT

JSON schema files (`*.schema.json`) in this directory are the Contract SSOT.
They define canonical data shapes for cross-component boundaries.
Do NOT duplicate schema definitions elsewhere.
