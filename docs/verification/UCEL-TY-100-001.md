# UCEL-TY-100-001 Verification

## Changed files
`git status --short | awk '{print $2}' | rg -v 'node_modules'`

- ucel/crates/ucel-testkit/Cargo.toml
- ucel/crates/ucel-testkit/src/lib.rs
- ucel/crates/ucel-testkit/src/golden_manifest.rs
- ucel/crates/ucel-testkit/src/support_bundle_manifest.rs
- ucel/crates/ucel-testkit/tests/strict_golden_manifest_gate.rs
- ucel/crates/ucel-testkit/tests/support_bundle_manifest_gate.rs
- ucel/crates/ucel-transport/src/diagnostics/support_bundle.rs
- ucel/crates/ucel-transport/tests/support_bundle_manifest.rs
- ucel/fixtures/golden/manifest.json
- ucel/fixtures/golden/ws/*/raw.json + expected.normalized.json (strict venues missing pair files)
- ucel/fixtures/support_bundle/manifest.json
- scripts/ucel/update_golden_manifest.py
- scripts/ucel/update_support_bundle_manifest.py
- docs/specs/ucel/ty_100_definition_spec_v1.md
- docs/status/trace-index.json
- docs/verification/UCEL-TY-100-001.md

## What / Why
- **T**: Added a deterministic golden manifest gate that validates strict venue required files, hash/byte integrity, deny-pattern redaction, and fixture size bounds.
- **Y**: Added a support-bundle manifest contract and ensured generated transport support bundles embed that manifest and satisfy required reproducibility keys.
- This shifts strict golden checks from existence-only to structure + content consistency checks, and supportability from shape-only to machine-verifiable manifest checks.

## Self-check results
- Allowed-path check: OK
- Tests added/updated:
  - `ucel/crates/ucel-testkit/tests/strict_golden_manifest_gate.rs`
  - `ucel/crates/ucel-testkit/tests/support_bundle_manifest_gate.rs`
  - `ucel/crates/ucel-transport/tests/support_bundle_manifest.rs`
- Commands:
  - `python3 scripts/ucel/update_golden_manifest.py --accept` => OK (`WROTE ... (62 files)`)
  - `python3 scripts/ucel/update_golden_manifest.py` => OK (`golden manifest is up to date`)
  - `python3 scripts/ucel/update_support_bundle_manifest.py` => OK (`normalized ...`)
  - `cargo test --manifest-path ucel/Cargo.toml -p ucel-testkit --test strict_golden_manifest_gate` => FAIL (pre-existing workspace manifest error: duplicate `ucel-transport` dependency in `ucel/crates/ucel-cex-sbivc/Cargo.toml`)
  - `cargo test --manifest-path ucel/Cargo.toml -p ucel-testkit --test support_bundle_manifest_gate` => FAIL (same workspace blocker)
  - `cargo test --manifest-path ucel/Cargo.toml -p ucel-transport --tests` => FAIL (same workspace blocker)
  - `cargo fmt --manifest-path ucel/Cargo.toml --all` => FAIL (same workspace blocker)
  - `rustfmt <changed rust files>` => OK
- trace-index validation:
  - `python3 -m json.tool docs/status/trace-index.json > /dev/null` => OK
- Secrets scan（簡易）:
  - `rg -n "AKIA|BEGIN PRIVATE KEY|Authorization: Bearer|X-Api-Key|SECRET|TOKEN|API_KEY" -S ucel docs || true`
  - Result includes expected policy/test/docs literals; no production secrets were added.

## ★履歴確認の証拠
- `git log --oneline --decorate -n 50`: latest intent chain includes strict golden existence gate (`2219d58`), support bundle foundation (`14fc4d4`), fuzz/chaos hardening (`4739bd7`, `018c91b`).
- `git log --graph --oneline --decorate --all -n 80` and `git log --merges --oneline -n 30`: confirms this task is additive to existing PRs #419/#420/#421/#423/#425.
- `git show --stat --oneline HEAD`: current baseline is PR #425 (strict market-data coverage completion), so this task extends proof strength rather than conflicting with coverage rollout.
- `git merge-base HEAD origin/master`: unavailable because no remotes are configured in this environment; used local `work` branch baseline instead.
- `git branch -vv` / `git reflog -n 30`: confirms branch created from `work` at `9035aa6` with no divergent hidden history.
- `git log --oneline -n 30 -- ucel/crates/ucel-testkit ucel/crates/ucel-transport`: confirms prior direction toward strict gates + supportability and this task is consistent with that policy.
- `git blame -w ucel/crates/ucel-testkit/src/ssot_gate.rs`: shows strict policy evolution (`2219d58`) and additive gate conventions.
- `git blame -w ucel/crates/ucel-transport/src/support_bundle.rs`: shows support bundle contract was introduced in one cohesive change (`14fc4d4`), so embedding fixture manifest is a local additive extension.
- Why manifest approach is conflict-safe: only adds sidecar manifests/scripts/tests and localized diagnostics payload extension; avoids broad fixture rewrites and preserves existing strict gate behavior while adding stronger checks.
