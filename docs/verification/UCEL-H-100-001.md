# UCEL-H-100-001 Verification

## Changed files
`git diff --name-only`:

- docs/exchanges/sbivc/catalog.json
- docs/specs/ucel/market_data_coverage_scope_policy_v1.md
- docs/status/trace-index.json
- docs/verification/UCEL-H-100-001.md
- ucel/Cargo.lock
- ucel/coverage/deribit.yaml
- ucel/coverage/sbivc.yaml
- ucel/crates/ucel-cex-deribit/Cargo.toml
- ucel/crates/ucel-cex-deribit/tests/coverage_contract.rs
- ucel/crates/ucel-cex-sbivc/Cargo.toml
- ucel/crates/ucel-cex-sbivc/src/lib.rs
- ucel/crates/ucel-cex-sbivc/tests/coverage_contract.rs
- ucel/crates/ucel-testkit/tests/coverage_h_100_gate.rs
- ucel/fixtures/golden/ws/deribit/expected.normalized.json
- ucel/fixtures/golden/ws/deribit/raw.json
- ucel/fixtures/golden/ws/sbivc/expected.normalized.json
- ucel/fixtures/golden/ws/sbivc/raw.json

## What / Why
- deribit/sbivc の coverage が stale（false 固定 / entries 空）で、実装実態と乖離して H 完成度を過小評価していた。
- H（Market Data）coverage の範囲（public market_data のみ）を policy v1 として SSOT 化し、private/trading（I）混入で 100% 定義が揺れる問題を固定した。
- deribit/sbivc coverage を strict=true + entries 全件 implemented/tested=true に更新し、coverage⇔catalog⇔gate の整合テストを追加した。
- strict venue fixture 規約（raw.json/expected.normalized.json）を deribit/sbivc に追加し、将来 gate 強化でもパス固定で対応可能にした。

## Self-check results
- Allowed-path check: OK
- Tests added/updated:
  - ucel/crates/ucel-cex-deribit/tests/coverage_contract.rs
  - ucel/crates/ucel-cex-sbivc/tests/coverage_contract.rs
  - ucel/crates/ucel-testkit/tests/coverage_h_100_gate.rs
- Commands:
  - cargo test --manifest-path ucel/Cargo.toml -p ucel-cex-deribit => PASS
  - cargo test --manifest-path ucel/Cargo.toml -p ucel-cex-sbivc => PASS
  - cargo test --manifest-path ucel/Cargo.toml -p ucel-testkit --test coverage_h_100_gate => PASS
  - cargo fmt --manifest-path ucel/Cargo.toml --all => PASS
- trace-index json.tool:
  - python -m json.tool docs/status/trace-index.json > /dev/null => PASS
- Secrets scan（簡易）:
  - rg -n "AKIA|BEGIN PRIVATE KEY|SECRET|TOKEN|API_KEY" -S <changed-files> || true => PASS（実データ/秘密情報の追加なし）

## ★履歴確認の証拠
- `git log --oneline --decorate -n 50` / `git log --graph --oneline --decorate --all -n 80` / `git log --merges --oneline -n 30`:
  - 直近で coverage strict 化（PR #423, commit 2219d58）が入っており、本タスクはその方針を deribit/sbivc の stale 部分へ適用する整合修正。
- `git show HEAD`:
  - 直前 HEAD は UCEL-I 実行系拡張（#424）で H coverage と競合しない。
- `git merge-base HEAD work`:
  - merge-base は `57399f9`（作業分岐元一致）。
- `git branch -vv` / `git reflog -n 30`:
  - `work` から `feature/ucel-h-100-001` を分岐した記録を確認。
- `git log --oneline -n 30 -- ucel/coverage/deribit.yaml ucel/coverage/sbivc.yaml`:
  - ee40194 で v1 schema 化後、deribit false 群 / sbivc entries:[] が残存していた。
- `git blame -w ucel/coverage/deribit.yaml` / `git blame -w ucel/coverage/sbivc.yaml`:
  - deribit は Step2 target コメント付き false 群、sbivc は entries:[] のまま止まっていた事実を確認。
- `git log --oneline -n 30 -- ucel/crates/ucel-cex-deribit ucel/crates/ucel-cex-sbivc`:
  - 実装側は c6d2b43 / 2739603 以降進んでおり coverage stale と乖離。
- `cat ucel/coverage/{deribit,sbivc}.yaml`, `ls ucel/crates/.../src`, `ls docs/exchanges/{deribit,sbivc}`:
  - coverage と実装/カタログの不一致、および sbivc catalog 空状態を確認。
- 追加ゲートの fail-fast 根拠:
  - catalog に無い ID、entries 空、strict=false、implemented/tested false、fixture 欠落のいずれでもテスト失敗する設計。

## Environment note
- `origin/<default-branch>` はこのローカル clone に remote が無く取得不可（`git branch -r` が空）。
  代替として `work` ブランチ基準の merge-base/diff/log で競合兆候を確認した。
