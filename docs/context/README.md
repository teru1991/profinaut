# docs/context/ (Operator + AI Entry Hub)
このディレクトリは **運用者/AI のための導線（入口）**です。
ここには「正（SSOT）の定義」は置きません。正本は必ず以下にあります：

- 契約SSOT: `docs/contracts/`
- 固定仕様（Core Spec）: `docs/specs/`
- Policy: `docs/policy/`
- Plan: `docs/plans/`
- Runbook: `docs/runbooks/`

## Canonical operator entrypoint
- `docs/context/README_AI.md` が、作業開始前の必読エントリです。

## このディレクトリに置いてよいもの
- 読む順番のガイド
- リンク集（Tech context hub）
- 実行プロンプトテンプレ
- チェックリスト（ただし正本の再定義は禁止：リンクのみ）

## 禁止
- 契約（schema）の再掲/改変をここに置く
- Core Spec の意味（状態/境界/不変条件）をここで定義する
