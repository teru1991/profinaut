# ============================================================================
# Core Settings
# ============================================================================
PROJECT_NAME=profinaut
ENVIRONMENT=local
COMPOSE_PROJECT_NAME=profinaut-dataplat

# Auth MVP (replaceable later)
ADMIN_TOKEN=change-me-local-admin-token

# ============================================================================
# Database (Postgres)
# ============================================================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5433
POSTGRES_DB=profinaut
POSTGRES_USER=profinaut_app
POSTGRES_PASSWORD=change-me-postgres
DATABASE_URL=postgresql://profinaut_app:change-me-postgres@postgres:5432/profinaut
POSTGRES_MEM_LIMIT=768m
POSTGRES_CPUS=1.0

# ============================================================================
# Time-Series Database (ClickHouse)
# ============================================================================
CLICKHOUSE_DB=profinaut
CLICKHOUSE_USER=profinaut_app
CLICKHOUSE_PASSWORD=change-me-clickhouse
CLICKHOUSE_HTTP_PORT=8123
CLICKHOUSE_NATIVE_PORT=9001
CLICKHOUSE_MEM_LIMIT=2g
CLICKHOUSE_CPUS=2.0

# ============================================================================
# Cache (Valkey/Redis)
# ============================================================================
VALKEY_PASSWORD=change-me-valkey
VALKEY_PORT=6379
VALKEY_MEM_LIMIT=512m
VALKEY_CPUS=0.75

# ============================================================================
# Object Storage (SeaweedFS)
# ============================================================================
OBJECTSTORE_ACCESS_KEY=change-me-objectstore-access
OBJECTSTORE_SECRET_KEY=change-me-objectstore-secret
OBJECTSTORE_REGION=us-east-1
OBJECTSTORE_BUCKETS=profinaut-bronze,profinaut-silver,profinaut-gold,profinaut-mlflow
SEAWEEDFS_MASTER_PORT=9333
SEAWEEDFS_FILER_PORT=8888
SEAWEEDFS_S3_PORT=8333
OBJECTSTORE_MEM_LIMIT=512m
OBJECTSTORE_CPUS=1.0

# ============================================================================
# Domain B: Secrets / Identity / Access (Vault)
# ============================================================================
VAULT_ROOT_TOKEN=change-me-vault-root
VAULT_PORT=8200
VAULT_MEM_LIMIT=256m
VAULT_CPUS=0.5

# ============================================================================
# Domain C: Observability / SRE
# ============================================================================
# Prometheus
PROMETHEUS_PORT=9090
PROMETHEUS_RETENTION=30d
PROMETHEUS_MEM_LIMIT=1g
PROMETHEUS_CPUS=1.0

# Grafana
GRAFANA_PORT=3001
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-grafana
GRAFANA_MEM_LIMIT=512m
GRAFANA_CPUS=0.75

# Loki (Log Aggregation)
LOKI_PORT=3100
LOKI_MEM_LIMIT=512m
LOKI_CPUS=0.75

# ============================================================================
# Domain J: Risk / Policy Gate (Policy Decision Cache)
# ============================================================================
VALKEY_POLICY_PASSWORD=change-me-valkey-policy
VALKEY_POLICY_PORT=6380
VALKEY_POLICY_MAXMEMORY=256mb
VALKEY_POLICY_MEM_LIMIT=384m
VALKEY_POLICY_CPUS=0.5

# ============================================================================
# Domain N/P: Experiment / AI/ML Platform (MLflow)
# ============================================================================
MLFLOW_PORT=5000
MLFLOW_MEM_LIMIT=768m
MLFLOW_CPUS=1.0

# ============================================================================
# Domain O: Deterministic Replay / Audit Event Log (EventStore)
# ============================================================================
EVENTSTORE_TCP_PORT=1113
EVENTSTORE_HTTP_PORT=2113
EVENTSTORE_MEM_LIMIT=1g
EVENTSTORE_CPUS=1.0

# ============================================================================
# Domain T: Testing / Simulation / Chaos (Toxiproxy)
# ============================================================================
TOXIPROXY_API_PORT=8474
TOXIPROXY_MEM_LIMIT=256m
TOXIPROXY_CPUS=0.5

# ============================================================================
# Domain Q: On-chain Trading / Arbitrage (Hardhat Node - Optional)
# ============================================================================
HARDHAT_RPC_PORT=8545
HARDHAT_FORK_URL=
HARDHAT_FORK_BLOCK_NUMBER=
HARDHAT_MEM_LIMIT=1g
HARDHAT_CPUS=1.0

# ============================================================================
# Service Ports
# ============================================================================
DASHBOARD_API_PORT=8000
WEB_PORT=3000
DASHBOARD_API_URL=http://dashboard-api:8000

# ============================================================================
# Notifications (Phase 1)
# ============================================================================
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/placeholder

# ============================================================================
# Python Agent SDK (Step 4)
# ============================================================================
CONTROL_PLANE_URL=http://localhost:8000
COMMAND_PULL_URL=
COMMAND_FILE=sdk/python/commands.json
INSTANCE_ID=inst-local-1
BOT_ID=bot-local-1
RUNTIME_MODE=PAPER
EXCHANGE=BINANCE
SYMBOL=BTCUSDT
AGENT_VERSION=0.1.0
HEARTBEAT_INTERVAL_SECONDS=30
DEADMAN_TIMEOUT_SECONDS=90
DEADMAN_ACTION=SAFE_MODE
